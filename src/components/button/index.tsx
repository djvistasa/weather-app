/**
 *
 * Button
 *
 */

import React from 'react';
import { StyledButton } from './styles';
import { IButtonProps } from './types';

function Button(_props: IButtonProps): JSX.Element {
  return <StyledButton></StyledButton>;
}

export default Button;
