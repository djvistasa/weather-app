import { render } from '@testing-library/react-native';
import React from 'react';
import {{ properCase name }} from '.';

test('does component render', () => {
  const tree = render(<{{ properCase name }} />);
  tree;
});
